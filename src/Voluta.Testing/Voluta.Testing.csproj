<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Description>Test doubles and helpers for Voluta (recording/fault checkpointers, stream capture, graph fixtures, checkpoint conformance).</Description>
    <PackageTags>$(PackageTags);testing;test-doubles;conformance</PackageTags>
    <!-- Not shipped until intentionally published (quality-engineering packability policy). -->
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Voluta.Abstractions\Voluta.Abstractions.csproj" />
    <ProjectReference Include="..\Voluta\Voluta.csproj" />
  </ItemGroup>

</Project>
