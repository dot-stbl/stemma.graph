// Marker for the Voluta assembly. Real runtime types — StateGraph<T>,
// CompiledGraph, channels, reducers, conditional edges, streaming — land in
// subsequent PRs. See https://github.com/dot-stbl/voluta — CLAUDE.md.

namespace Voluta;

/// <summary>
///     Marker type for the Voluta assembly. Real public surface lands in
///     subsequent PRs during the MVP implementation.
/// </summary>
public static class AssemblyMarker;
