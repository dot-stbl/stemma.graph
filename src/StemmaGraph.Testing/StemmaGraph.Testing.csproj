<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Description>Test doubles and helpers for StemmaGraph (recording/fault checkpointers, stream capture, graph fixtures, checkpoint conformance).</Description>
    <PackageTags>$(PackageTags);testing;test-doubles;conformance</PackageTags>
    <!-- Not shipped until intentionally published (quality-engineering packability policy). -->
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\StemmaGraph.Abstractions\StemmaGraph.Abstractions.csproj" />
    <ProjectReference Include="..\StemmaGraph\StemmaGraph.csproj" />
  </ItemGroup>

</Project>
